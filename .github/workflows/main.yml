name: run

on:
  workflow_dispatch:
jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: CHeckout this repo
        uses: actions/checkout@4

      - name: Set up python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11
